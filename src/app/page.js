import LoginLinks from '@/app/LoginLinks'

export const metadata = {
    title: 'PhraseWave',
}

const Home = () => {
    return (
        <>
            <LoginLinks />
        </>
    )
}

export default Home
